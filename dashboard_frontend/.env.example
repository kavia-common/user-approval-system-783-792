# Base URL for the backend API (FastAPI). Example: http://localhost:3001
REACT_APP_API_BASE_URL=http://localhost:3001

# Optional: key used to store JWT token in localStorage
REACT_APP_AUTH_TOKEN_KEY=auth_token
